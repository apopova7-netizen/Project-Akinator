 // if (lineNumber != 2) {
            //     printf("Error in line %d: root must be on line 2 (right after statistics)\n", lineNumber);
            //     fclose(file);
            //     return 0;
            // }